{
  "name": "web-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Личный HTTP(S) + SOCKS5 прокси с авторизацией по логину/паролю",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=16"
  }
}
