{
  "name": "web-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Личный HTTP(S) + SOCKS5 прокси с авторизацией по логину/паролю",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=16"
  }
}
